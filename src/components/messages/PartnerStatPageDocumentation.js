import React from 'react'

const PartnerStatPageDocumentation = () => (
  <div className='doc-div'>
    <h2>Completion Stats Page</h2>
    <p>The Completeness Stats Page displays the statistics of the proposals by proposal based on the observations completeness by priorities.</p>
    <p>Each of the proposal shows a status and the comment where necessary which details the reason for that proposal status.</p>
    <p>There are proposals which are not yet observed. Those proposal are highlighted with the color red showing zero percentages for all the observations completeness.</p>
  </div>
)

export default PartnerStatPageDocumentation
