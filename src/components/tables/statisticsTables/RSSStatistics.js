import React from "react";
import propTypes from "prop-types";

const RSSStatistics = (proposals) => {
    return(
        <div>
        
        </div>
    )
};

RSSStatistics.propTypes = {
    proposals: propTypes.array.isRequired,
}

export default RSSStatistics;